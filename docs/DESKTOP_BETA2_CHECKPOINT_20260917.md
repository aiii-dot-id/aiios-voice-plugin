# Desktop beta.2 checkpoint — 2026-09-17

## Active release authority and execution

### 2026-09-17 — host requests and private engine repair

The host acknowledged engine ownership and landed acquisition retirement; our
three unchanged race cases pass on `12c68b38`. A new unsigned Mac worker/ASR
candidate passes current-reply interruption and real five-model SDK speech,
including opening words, VAD barge-in, recovery and Finish/drain. The two exact
harmless ONNX notices are suppressed at source; other diagnostics remain.
This does not repair the immutable signed beta.2 or close its browser gate.

Host/kit now own the agreed package startup allowance, operator precedence and
ceiling. Voice measured Linux's dominant startup cost in full-model portable
SHA256; an accelerated candidate has integrity proof but not yet a measured
startup win or Ubuntu 24.04 distribution closure. Further dev8 load waits for
the host gate's explicit hand-back. The [engine checkpoint](../deliverables/engine-startup-interruption-20260917-r1/README.md)
records executed evidence, retained harness failures and the exact next gates.

### 2026-09-17 12:10Z — browser integration reopened the voice candidate

The exact signed `4e6c215f…` package fails the real Mac browser interruption
join on clean host `601b9d71`: the host sends an empty synthesis ID to select
the current generation, but the native worker requires a nonempty string.
Local browser playback stops while native synthesis continues and terminal
stopped receipts refuse. The package remains immutable evidence, **not a
catalog-ready candidate**. This newly measured defect supersedes the engine
freeze as a no-further-changes decision; models, settings and host ownership
remain unchanged.

Voice owns the repair. Fifteen native-worker contract tests now pass after
four intended baseline assertion failures, including an adjacent null-receipt
identity defect. Rebuilt desktop companions, rebound carriers, a newly signed
package and repaired real-browser/installed proofs are still required; these
unit/protocol results do not update the shipped bytes or qualify the release.
Evidence and the host handoff are under
`deliverables/beta2-browser-join-20260917-r1/`.

This is the current desktop release entry point. Older checkpoint documents
are retained history, not competing candidate selections. The operator resumed
execution on September 17 and assigned voice-platform decisions to the voice
agent, with host/SDK changes handed to the Go AII OS agent through the exchange.

- **A — integrated candidate:** host adopts the delivered UID privacy/order/
  retirement and acquisition-cancellation patches, separates background
  acquisition from resident readiness (no total download deadline), and
  completes guided enrollment, safe model-bound profile upgrade and AI policy
  proposal/readback. Return the clean host commit and exact routes/config.
- **B — installed use:** run the complete fresh-cache browser journey below on
  Mac, live dev7 Ubuntu 24.04 and the accepted Windows VM. No preseeded fixture
  or supplied download callback is called a public-download/browser pass.
- **C — publication:** upload immutable beta.2 assets and the catalog only after
  the applicable acceptance. The signed package points to final public release
  URLs, so those downloads cannot be proved before the assets exist: first
  complete local/contained host acceptance, then upload an explicitly labelled
  prerelease asset set without catalog promotion, run exact public-URL fresh-cache
  installed journeys, and only then publish the catalog entry. A downloadable
  prerelease candidate is not an accepted catalog release. Never replace beta.1.
- **D — mobile and quality:** the voice owner completes actual Pixel/iPhone
  realtime, accelerator, physical-audio and sustained qualification, and uses
  independent human evaluation to justify wider claims. This work does not
  silently change the frozen desktop release.

Voice engine/model choices remain frozen at the signed package below. Host
implementation belongs to its agent; posted requests are not landed work.
The executable integration request, including acceptance and prior patch hashes,
is `dev8:/work/aiii/exchange/to-host/20260917-1115-execute-desktop-beta-milestones.md`.
The acquisition design was revised by agreement at 11:31Z: bounded HTTP
requests, retry/backoff, progress and root cancellation under the host lifetime,
not a new total-budget setting. This supersedes that part of the 1115 handoff.
No live identity configuration is overwritten.

### Host integration receipt — 2026-09-17 11:31Z

The host owner acknowledged ownership and landed acquisition plus the earlier
cancellation repair on clean main `601b9d71` (AII OS 0.1.6). Its new source
separates acquisition from activation and exposes preparing/progress state.
The host reports a complete passing gate; this agent has independently read
the landed source, not rerun its broad gate.

A new independent source-overlay probe then reproduced a P1 on that revision:
rapid A→B→C replacement, Forget→Want, and Keep→Want each allow a new download
to open the same partial file before the original canceled fetch has retired.
All three cases fail by their intended assertions; hash admission bypass is
not claimed. The test explicitly releases teardown and requires eventual
successor progress. Evidence and requested ownership-safe repair were delivered
to `dev8:/work/aiii/exchange/to-host/20260917-1131-acquisition-retirement-probe-and-decision.md`.
Acceptance of the background lifetime design does not close this race.

The host owner next owns that repair, integrated UID adoption, guided enrollment,
policy UI and safe existing-profile upgrade. The final installed beta.2 journey
must use those landed bytes. The host's current catalog-install instruction is
for beta.1; its results must not be counted as acceptance of beta.2's changed UID.

### Execution receipt — 2026-09-17 11:21Z

The exact package's six speaker operations and seven settings now pass the
production host loaders/validators on macOS and dev8 Linux; every advertised
speaker example validates, required/unknown argument refusals execute, all ten
voice choices load, and unsupported language/fractional pause values refuse.
All three actual runtime archives also pass fresh materialization, interrupted
download resume, verified offline reuse, corrupt-download refusal and installed
manifest/spawn tamper refusal on each host. These are two top-level tests and
nine subtests per host, zero failures/skips. Production T3 verification and an
appended-byte refusal run in the same probe.

This uses the isolated acquisition-repair checkout at base `93ccab9c`, not a
main landing. Download callbacks supply local release bytes; no native engine,
browser, physical device or public HTTP claim follows. No production Go file
was edited by the probe; the external test is supplied through a Go overlay.
Evidence: `deliverables/desktop-beta-execution-20260917-r1/`.

All twelve release assets are copied to and independently hash-checked at
`dev8:/work/aiii/work/voice-frontier/desktop-beta2-notice-publication-20260917-r1/`.
The host owner now has the complete 297,260,044-byte release set, not just the
package and metadata. The separate distribution addendum remains adjacent at
`desktop-beta2-distribution-review-20260917-r2/`. No upload or live install.

**11:40Z native Windows follow-through:** the unchanged probe passes six speaker
contracts, seven settings and the Windows runtime's fresh/resume/offline/
corruption/tamper cases on the actual VM: two top-level tests, seven subtests,
zero failures/skips. Source is a frozen, 1,554-file verified export of landed
`601b9d71`, not an edited live checkout. Test preparation/execution takes
140.484 seconds; the child retires and private temporary files are cleared.
The test uses local byte callbacks in session 0; this does not certify audio,
GPU, AppContainer execution, browser use or public downloading. Its raw-event
readback is independently checked under the same execution evidence directory.

## Achieved

One signed, source/evidence-bound integration candidate for macOS Apple Silicon,
Ubuntu x86-64, and Windows x86-64, with the review repairs integrated and a
measured commercial-friendly UID replacement. This is an executed desktop
engine/package milestone, not a declaration that all installed/browser or
five-platform human-quality release gates passed.

Authoritative publication inputs, refreshed with exact Intel notices:
`/Volumes/AIII Models/work/voice-frontier/deliverables/desktop-beta2-notice-publication-20260917-r1/`.

- Package: `assets/id.aiii.voice-0.1.0-beta.2.aiiospkg`, 9,420,592 bytes.
- SHA-256: `4e6c215fb6bec5ec01031c4b0b0229d09104e969834bcf5b4ad4ff3ca1083479`.
- T3 authority: `aiii_platform_release_t3_20260703_k15`.
- Actual clean AII OS verifier: `93ccab9c4db2b2611f713b069eb86568b1b9adff`.
- Verifier accepts the exact signed package and refuses the appended-byte
  falsifier. Signing changes no manifest or install-root member. Private keys
  remain with their existing dev8 owner; none entered these artifacts.
- One catalog entry has three platform rows pointing to this same archive.
  Twelve local release assets total 297,260,044 bytes. Seventeen additional
  pinned upstream model files passed fresh complete anonymous download hashes.
- Beta.1 was already public when checked. Its tag, assets and catalog were
  not overwritten. Beta.2 is **staged, not uploaded or installed**.

**2026-09-17 10:31Z distribution addendum:** the two remaining notice-index
questions are now resolved as reproducibility limitations, not demonstrated
redistribution restrictions. The exact Silero/Smart Turn declarations and
AsmJit wheel/submodule chain bind the shipped bytes and already-present notices;
all 73 notices verify. The source-bound addendum is
`deliverables/desktop-beta2-distribution-review-20260917-r2/publication-addendum.json`.
It accompanies, rather than rewrites, the immutable signed package and original
publication plan. This is a scoped engineering disposition, not blanket legal
clearance or installed release qualification; the technical gates below stand.

## Executed desktop evidence

The recorded execution below remains bound to signed archive `8b822e2c…`.
The new `4e6c215f…` archive has only notice additions/index changes and a fresh
signature; all executable, model, runtime, settings and interface members are
identical. Its exact-package signature and tamper checks pass. A new installed
browser journey is still required; it does not inherit that label from its parent.

| Target actually tested | Native path retained | Recorded SDK result |
| --- | --- | --- |
| M3 Ultra macOS | Existing Metal/CPU profile | Guided enrollment after capture close and process restart; full speech/UID/VAD, interruption/opening words/recovery; 13 settings cases pass |
| Shared dev7 Ubuntu 24.04 laptop | Existing Vulkan/CPU profile | Same gates pass without stopping NoMachine/apps or changing drivers/audio routes |
| Windows 11 VM, GTX 1070 | Existing DirectML encoder, Vulkan TTS and CPU components | Same gates pass on the final Authenticode-signed bytes; 9 timestamped AIII images verified |

All thirteen TTS PCM hashes remain identical to each platform's accelerated
parent. This is per-platform parity, not cross-platform bit identity. Ten stable
voices, English STT/TTS, active adjustable VAD, Finish/drain, independent
interruption and recovery remain working in the recorded SDK journeys.

Windows final-byte measurements: cold start 51.945 s; warm first PCM 202.526
and 315.414 ms; synthesis RTF 0.5603 and 0.5333; guided Abort admission 2.665 ms.
The VM is the measured target; no assumed 10x native speedup is used. These
measurements do not include browser audio-device latency or a listening panel.
All owned inference processes retired, and this turn's temporary Windows tasks
were collected and removed. Failed earlier harness attempts remain evidence.

Exact independent audits, relative to the project root:

- Mac: `deliverables/commercial-uid-metal-sdk-20260917-r1/`.
- Ubuntu: `deliverables/commercial-uid-linux-20260917-r3/`.
- Windows final signed bytes: `deliverables/commercial-uid-signed-windows-20260917-r1/independent-audit.json`.
- Repair integration and test dispositions: `docs/REVIEW_REPAIR_INTEGRATION_20260917.md`.
- Final packaging/tool tests: `deliverables/desktop-beta2-candidate-20260917-r2/release-tools-tests.xml` (34 passed).

## Additional real-host qualification, 2026-09-17 04:20Z

The exact signed beta.2 now also passes the real production host's activation,
containment and recorded-speech journey on all three desktops at both 24 and
48 kHz speaker rates. All 14 challenge words survive, active speech is
interrupted, recovery and the final tail complete, and owned processes retire.
Evidence: `deliverables/beta2-host-activation-20260917-r2/README.md`.
The Windows temporary task was collected and removed; live identities remain
untouched. This advances the boundary from SDK/offline verification to actual
contained host execution, but uses preseeded assets and does not certify the
browser, public-download path or installed enrollment UI.

Two host UID-policy defects were reproduced during this qualification:
JSON null can silently replace a restricted policy with `all`, and asynchronous
UID decisions can release a later held utterance before an earlier one. A
candidate now refuses null policies/fields and invalid inactive values, and
preserves both page and turn-admission order without waiting behind inference.
Ten regressions and four compiled mutations pass in the isolated checkout,
including tightened restrictions on approved but still-undelivered words;
both full partitioned and unsharded race runs pass after separate pre-existing
Mac fixture repairs (58 tested packages, 3,692 test/subtest passes unsharded).
Format/build/vet/matrix/staticcheck/cross-vet pass; the Mac host-installer stage
stopped at missing `dpkg-deb`, so the complete host release gate is not claimed.
Evidence: `deliverables/uid-host-order-20260917-r1/README.md`. Neither defect is
called closed on main. Host adoption, guided enrollment/AI policy surface and
safe old-model upgrade remain part of the release acceptance below.

## UID change and its limits

WeSpeaker VoxCeleb ResNet152-LM replaces the VoxBlink weights in beta.2 only.
Its model card declares Apache-2.0 and the upstream VoxCeleb checkpoint policy
declares CC-BY-4.0; both notices and attribution are included. No either/or
license choice or relicensing of other dependencies is asserted.

Same frozen selection panel and thresholds: 59/60 known-speaker queries correctly
identified (previous 58/60), zero wrong-identity acceptances, zero unknown
acceptances in 65 trials. Model size falls 21.5%; CPU inference over the 161
recordings falls 16.8%. Native embeddings agree with the reference.
This panel selected the model; it is not an independent blind benchmark or a
population/security guarantee. UID does not authorize commands.

The final package contains seven speaker input/output schemas, usable discovery
descriptions and examples. One explicit capture can be stopped and confirmed
after a restart. A host browser enrollment journey is a separate proof from
that SDK/storage fixture.

## Download and installation inputs

| Target | Runtime archive | Installed runtime | Selected model data |
| --- | ---: | ---: | ---: |
| macOS arm64 | 12,254,227 B | 41,030,498 B | 3,062,640,008 B |
| Ubuntu x86-64 | 22,339,678 B | 68,367,760 B | 3,062,640,008 B |
| Windows x86-64 | 112,783,399 B | 364,700,314 B | 3,062,640,008 B |

Every target selects its own runtime and 24 model files, not other platforms'
executables. Space excludes caches, partial downloads, private state and
side-by-side update/rollback copies. No system Python or source checkout is
required. Existing AII OS owns download, verification, installation and grants.

`publication-plan.json`, `catalog-entry.json`, `SHA256SUMS`,
`upstream-download-evidence.json`, and `operator-setup.json` carry the exact
machine-readable inputs. Linux and Windows require the already-supported,
operator-owned 180000 ms startup allowance; Mac retains its default. Nothing
automatically edits the identity's configuration.

## Concrete remaining acceptance, owners and ordering

1. **Host owner + voice integration:** prove the existing single-owner guided
   enrollment UI and AI-visible `only`/`ignore` policy on a fresh test identity.
   Withholding must happen before partial/final transcript fan-out, recording
   or model delivery; restrictive filters must not degrade to unrestricted.
2. **Host private-storage owner + voice integration:** implement explicit,
   operator-confirmed old-model backup/reset/re-enrollment before updating an
   existing identity. Both enrollment and pending capture files are model-bound.
   New-model `speaker.reset` cannot read the old profile; old-model reset leaves
   an old-policy empty document. Preserve originals/rollback and restrictive
   policies. No automatic deletion, vector relabeling, or lost pending capture.
3. **Voice + host release:** exact signed fresh-cache acquisition and contained
   browser journey on all three desktops: resume/integrity refusal, settings,
   guided enrollment, known/unknown filtering, barge-in/opening words/recovery,
   Finish/Abort/reopen and rollback. These new bytes have SDK and contained-host
   proof, not this installed browser/public-download release proof.
4. **Distribution owner:** the Windows OpenMP distribution question is now
   resolved against Intel's own NuGet redistributable: the DLL exactly matches
   the final runtime, with the Simplified Software License and third-party
   notices. The assembler includes these texts; the notice-only candidate is
   now T3 signed, verifier-accepted and catalogued locally under
   `deliverables/desktop-beta2-notice-publication-20260917-r1/`. It is not uploaded.
   Its fresh T3 signature and verification are complete; no runtime was rebuilt.
   See `deliverables/intel-openmp-redistribution-20260917-r1/README.md`.
   The retained Silero/Smart Turn export source and AsmJit build-provenance
   limitations remain explicit. The 10:31Z addendum resolves their distribution
   disposition: the reviewed notices do not require reproducing those exports
   or builds, and the exact required notices already ship. They are not new
   noncommercial blockers. Other components retain their own terms; this is
   not relicensing the dependency tree.
5. **Authorized release publisher:** upload new beta.2 assets only after the
   acceptance above, integrate/sign the catalog, and independently read back
   public bytes. Never replace beta.1 bytes under its existing tag.

Android/iPhone and human-level listening/accuracy qualification are not covered
by this desktop checkpoint. There is no new NPU or physical-mobile claim.

Method: CONVERGE for repaired desktop engine parity, new UID artifact binding,
Windows signed-byte execution, and package integrity/signature/download metadata.
ITERATE for installed release acceptance and safe existing-enrollment upgrade.
