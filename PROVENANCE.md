# Provenance

Derived 2026-09-16T23:19:30Z from the voice working tree at
repository head `057751d3f78f6a189e5ce56b7abc1d5d78c248a3` PLUS its uncommitted working files: the plugin
directory and most of the engine were untracked in that repository at
derivation (untracked top-level entries: :memory:.ses, configs, cuda_tts, deliverables, diagnostics, docs, eval, experiments, plugin, research, review-sdk-LZIQlk, runtime, scripts, tests).
That initial import was a working-file snapshot, not a reproduction of the
research repository's HEAD. Subsequent commits in this repository are the
source authority; do not attribute their code to the old research HEAD.

Initial import: 631 files; tree digest (sha256 over `<sha256>  <path>` lines,
sorted) `be19dca5ab2e3a7bcb2519b43c3c79be44fdcf1bf5330bc2ce65166e0955b664`.
That historical digest does not describe subsequent commits. The current
`MANIFEST.sha256` inventories tracked source files excluding the manifest
itself; Git binds the inventory. Build products, model weights and qualification
evidence remain separate. See `docs/SOURCE_CLOSEOUT_20260917.md` for the reviewed
integration, fresh validation and release boundaries.

Comments, fixtures and documentation were cleaned of private host names,
paths and person names; 35 files changed. Nothing that ships in
the plugin package was rewritten beyond comments and fixture paths.
